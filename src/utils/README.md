# src/utils

Utilitários compartilhados: leitura de config externa (`config/*.yml`), logging estruturado, helpers de data/timezone, geração de `_record_hash`.

Princípio: sem secrets hardcoded — usar Databricks secret scopes (ver ADR-0002).
