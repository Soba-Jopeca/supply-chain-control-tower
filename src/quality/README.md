# src/quality

Regras de qualidade de dados desacopladas da lógica de transformação: schema validation, uniqueness, null checks, referential integrity, business rules, quarentena.

Fase: 6 (Data Quality).

Produz: `valid_records`, `invalid_records`, `quarantine_records`, `quality_metrics` (completeness, validity, uniqueness, consistency, freshness).
